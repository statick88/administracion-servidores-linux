---
title: "Unidad 10.3: SSL/TLS con Certbot"
subtitle: "HTTPS automático con Let's Encrypt"
author: "Diego Saavedra"
date: "2026-03-31"
date-format: medium
---

# Unidad 10.3: SSL/TLS con Certbot

## Introducción

Let's Encrypt ha revolucionado el SSL con certificados gratuitos y automáticos. Certbot es la herramienta oficial que automatiza la emisión, instalación y renovación de certificados TLS.

## Fundamentos de SSL/TLS

### ¿Por qué SSL?

```
┌─────────────────────────────────────────────────────────────────┐
│                    SSL/TLS EN PRODUCCIÓN                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   SIN SSL                    CON SSL                            │
│  ┌─────────┐               ┌─────────┐                         │
│  │HTTP     │               │HTTPS    │                         │
│  │         │               │         │                         │
│  │User: admin             │User: admin                          │
│  │Pass: secret123   →   │Pass: *** (cifrado)                  │
│  └─────────┘               └─────────┘                         │
│                                                                  │
│   - Datos en texto plano        - Cifrado AES-256               │
│   - Vulnerable a sniffers      - Autenticación del servidor     │
│   - Sin validación identidad   - Certificados verificables     │
│   - Navegadores warn          - Verde en navegador             │
└─────────────────────────────────────────────────────────────────┘
```

### Protocolos SSL/TLS

| Protocolo | Seguridad | Estado |
|-----------|-----------|--------|
| **SSLv3** | Inseguro | ❌ Deprecated |
| **TLS 1.0** | Inseguro | ❌ Deprecated |
| **TLS 1.1** | Inseguro | ❌ Deprecated |
| **TLS 1.2** | Seguro | ✅ Recomendado |
| **TLS 1.3** | Muy seguro | ✅ Recomendado |

## Certbot: Instalación

### Instalar Certbot

```bash
# Actualizar
$ sudo apt update && sudo apt upgrade -y
# <1>

# Instalar certbot y plugin nginx
$ sudo apt install -y certbot python3-certbot-nginx
# <2>
```

1. **apt update**: Lista de paquetes actualizada
2. **certbot**: Herramienta oficial Let's Encrypt

### Verificar Certbot

```bash
# Versión
$ certbot --version
certbot 2.1.0
# <1>

# Help
$ certbot --help all
```

1. **Versión**: Verificar instalación exitosa

## Obtener Certificado

### Requisitos Previos

:::{.callout-warning}
## ⚠️ ADVERTENCIA CRÍTICA

**Para obtener un certificado real, el servidor debe ser accesible públicamente.**

**Lo que podría salir mal:**
- DNS no apunta al servidor
- Firewall bloquea puertos 80/443
- Servidor en red local sin port-forward

**Cómo prevenirlo:**
1. Verificar DNS: `dig +short tu-dominio.com`
2. Abrir puertos: `sudo ufw allow 80/tcp && sudo ufw allow 443/tcp`
3. Probar HTTP público antes de certbot
:::

```bash
# 1. Verificar DNS
$ dig +short example.com
203.0.113.10
# <1>

# 2. Abrir firewall
$ sudo ufw allow 80/tcp
$ sudo ufw allow 443/tcp
$ sudo ufw status

# 3. Probar HTTP público
$ curl -I http://example.com
HTTP/1.1 200 OK
# <2>
```

1. **DNS**: Dominio apunta a IP del servidor
2. **HTTP**: Respondiendo públicamente

### Ejecución de Certbot

```bash
# Obtener certificado (modo interactivo)
$ sudo certbot --nginx -d example.com -d www.example.com
# <1>

# Following prompts:
# Saving debug log to /var/log/letsencrypt/letsencrypt.log
# Enter email address: admin@example.com
# Please read the Terms of Service at: https://letsencrypt.org/documents/LE-SA-v1.4-April-3-2024.pdf
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# Would you be willing to share your email address with the Electronic Frontier Foundation?
# (A)gree/(C)ancel: A
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# IMPORTANT NOTES:
#  - If you lose your account credentials, you can recover your
#    account credentials at 'https://letsencrypt.org/docs/accounts/'
#  - Your account credentials have been saved in /etc/letsencrypt
#  - Your certificate and chain have been saved at:
#    /etc/letsencrypt/live/example.com/fullchain.pem
#  - Your key file has been saved at:
#    /etc/letsencrypt/live/example.com/privkey.pem
#  - Your certificate will expire on 2026-06-30.
# <2>
```

1. **--nginx**: Auto-configura Nginx
2. **Certificados guardados**: En /etc/letsencrypt/

### Opciones Automáticas

```bash
# No interactivo (para scripts)
$ sudo certbot --nginx -d example.com \
    --email admin@example.com \
    --agree-tos \
    --redirect \
    --non-interactive
# <1>

# Múltiples dominios
$ sudo certbot --nginx -d example.com -d www.example.com -d api.example.com
```

1. **Sin interacción**: Para automation

### Múltiples Certificados

```bash
# Certificado para otro dominio
$ sudo certbot --nginx -d otherdomain.com
# <1>

# Ver todos los certificados
$ sudo certbot certificates
# <2>
```

1. **Otro dominio**: Certificado separado
2. **Listar**: Ver todos los certificados

## Configuración Generada

### Archivos de Certificado

```bash
# Ver estructura
$ sudo ls -la /etc/letsencrypt/live/example.com/
-rw-r--r-- 1 root root 0 Mar 31 10:00 cert.pem       # Certificado
-rw-r--r-- 1 root root 0 Mar 31 10:00 chain.pem      # Chain
-rw-r--r-- 1 root root 0 Mar 31 10:00 fullchain.pem  # cert + chain
-rw-r--r-- 1 root root 0 Mar 31 10:00 privkey.pem    # Clave privada
# <1>
```

1. **Archivos SSL**: Cert, chain, fullchain, privkey

### Configuración Nginx

```bash
# Ver configuración creada
$ cat /etc/nginx/sites-available/default-ssl.conf
# <1>
```

```nginx
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name example.com www.example.com;

    ssl_certificate /etc/letsencrypt/live/example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;

    # Redirect HTTP to HTTPS
    if ($scheme != "https") {
        return 301 https://$host$request_uri;
    }
}
```

### Configuración SSL Adicional

```bash
# Ver archivo de opciones
$ cat /etc/letsencrypt/options-ssl-nginx.conf
# <1>
```

```nginx
# SSL Settings - generated by Certbot
ssl_protocols TLSv1.2 TLSv1.3;
ssl_prefer_server_ciphers off;
ssl_ciphers ...;
# ...
```

## Renovación Automática

### Verificar Renovación

```bash
# Test de renovación (dry-run)
$ sudo certbot renew --dry-run
Saving debug log to /var/log/letsencrypt/letsencrypt.log
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
Processing /etc/letsencrypt/renewal/example.com.conf
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
Simulating renewal of certificate for the following domains:
example.com www.example.com
- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
Congratulations, all simulated renewals succeeded.
# <1>
```

1. **Simulated renewal**: Todo OK

### Cronjob de Renovación

```bash
# Ver cronjob
$ sudo cat /etc/cron.d/certbot
# <1>

# Output:
# 0 */12 * * * root test -x /usr/bin/certbot -a \! -d /run/systemd/system && perl -e 'sleep int(rand(43200))' && certbot -q renew
```

1. **Cronjob**: Dos veces al día, con random delay

### Renovación Manual

```bash
# Forzar renovación (si está próximo a expirar)
$ sudo certbot renew
# <1>

# Ver fecha de expiración
$ sudo certbot certificates | grep -A2 "Certificate Name"
Certificate Name: example.com
    Expiry Date: 2026-06-30 10:00:00+00:00 (-15 days)
# <2>
```

1. **renew**: Renueva todos los certificados
2. **Expiry**: Verificar fecha de expiración

## SSL Manual (Sin Certbot)

### Generar Certificado Self-Signed

```bash
# Generar certificado auto-firmado
$ sudo openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
    -keyout /etc/ssl/private/nginx-selfsigned.key \
    -out /etc/ssl/certs/nginx-selfsigned.crt \
    -subj "/C=US/ST=Test/L=Test/O=Org/CN=localhost"
# <1>
```

1. **Self-signed**: Para testing interno

### Configuración SSL Manual

```bash
$ sudo tee /etc/nginx/snippets/ssl-params.conf <<'EOF'
# SSL Certificate
ssl_certificate /etc/ssl/certs/nginx-selfsigned.crt;
ssl_certificate_key /etc/ssl/private/nginx-selfsigned.key;

# SSL Protocolos (TLS 1.2 y 1.3)
ssl_protocols TLSv1.2 TLSv1.3;

# Ciphers seguros
ssl_prefer_server_ciphers off;
ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;

# HSTS
add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload";

# OCSP Stapling (optional)
ssl_stapling on;
ssl_stapling_verify on;
EOF
# <1>
```

1. **SSL seguro**: Configuración manual

### Usar en Server Block

```bash
$ sudo tee /etc/nginx/sites-available/ssl-manual.conf <<'EOF'
server {
    listen 443 ssl http2;
    server_name example.com;
    
    include /etc/nginx/snippets/ssl-params.conf;
    
    root /var/www/html;
    index index.html;
    
    location / {
        try_files $uri $uri/ =404;
    }
}
EOF
# <1>
```

1. **Usar snippet**: Configuración SSL manual

## Testing SSL

### Verificar con curl

```bash
# Ver detalles SSL
$ curl -I https://example.com
HTTP/1.1 200 OK
Server: nginx
Strict-Transport-Security: max-age=63072000
# <1>

# Con verbose
$ curl -v https://example.com 2>&1 | grep -E "SSL|TLS"
*  SSL connection using TLSv1.3 / TLS_AES_256_GCM_SHA384
*  ALPN, server accepted to use http/1.1
*  certificate subject: example.com
# <2>
```

1. **HSTS**: Header presente
2. **TLS 1.3**: Versión usada

### SSL Labs Test

```bash
# Abrir en navegador
$ echo "https://www.ssllabs.com/ssltest/analyze.html?d=example.com"
# <1>
```

1. **SSL Labs**: Test completo público

## Casos de Estudio

### Caso 1: Nginx con Nuevo Dominio

**Escenario**: Agregar SSL a nuevo dominio.

```bash
# 1. DNS apuntando
$ dig +short newdomain.com
203.0.113.10

# 2. Instalar Nginx si no existe
$ sudo apt install -y nginx

# 3. Crear server block
$ sudo tee /etc/nginx/sites-available/newdomain.com <<'EOF'
server {
    listen 80;
    server_name newdomain.com www.newdomain.com;
    root /var/www/newdomain.com;
    index index.html;
}
EOF

$ sudo ln -sf /etc/nginx/sites-available/newdomain.com /etc/nginx/sites-enabled/
$ sudo nginx -t && sudo systemctl reload nginx

# 4. Obtener certificado
$ sudo certbot --nginx -d newdomain.com -d www.newdomain.com --redirect
# <2>
```

1. **DNS**: Verificar que apunta
2. **Certbot**: Obtener y configurar SSL

### Caso 2: Renovar Certificado Expirado

**Problema**: Certificado expirado, sitio mostrando advertencia.

```bash
# Ver estado
$ sudo certbot certificates
Certificate Name: example.com
    Expiry Date: 2024-03-01 10:00:00+00:00 (EXPIRED)
# <1>

# Renovar
$ sudo certbot renew
# <2>

# Recargar Nginx
$ sudo systemctl reload nginx
```

1. **EXPIRED**: Certificado vencido
2. **renew**: Renovar manualmente

### Caso 3: Múltiples Subdominios

**Escenario**: *.example.com con wildcard o múltiples certificados.

```bash
# Opción 1: Un certificado con múltiples dominios
$ sudo certbot --nginx -d example.com -d www.example.com -d blog.example.com -d shop.example.com

# Opción 2: Certificado wildcard (requiere DNS validation)
$ sudo certbot certonly --manual --preferred-challenges dns -d *.example.com
# <3>
```

1. **Múltiples**: Un certificado para todos
2. **Wildcard**: Todos los subdominios

## Resumen

| Tema | Comando Clave | Propósito |
|------|---------------|-----------|
| Instalar | `apt install certbot` | Herramienta TLS |
| Certificado | `certbot --nginx -d dominio` | Obtener certificado |
| Renewal | `certbot renew` | Renovar |
| Test | `certbot renew --dry-run` | Verificar renovación |
| SSL Manual | `openssl req -x509` | Certificado self-signed |

## Referencias

- [Let's Encrypt](https://letsencrypt.org/)
- [Certbot Documentation](https://certbot.eff.org/)
- [Mozilla SSL Configuration](https://ssl-config.mozilla.org/)
- [SSL Labs Test](https://www.ssllabs.com/ssltest/)
